<Window x:Class="MasterskayaOptronApplication.View.ApplicationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MasterskayaOptronApplication.View"
        mc:Ignorable="d"
        Closed="Window_Closed"
        Title="ApplicationWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Name="TextBlockFirstName" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding FirstName}" FontSize="25" Margin="30, 0"/>
        <TextBlock Grid.Row="1" Name="TextBlockMiddleName" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding MiddleName}" FontSize="25" Margin="30, 0"/>
        <TextBlock Grid.Row="2" Name="TextBlockLastName" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding LastName}" FontSize="25" Margin="30, 0"/>
        <TextBlock Grid.Row="3" Name="TextBlockPhone" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding Phone}" FontSize="25" Margin="30, 0"/>
    </Grid>
</Window>
